
spring.application.name=todo_service
# --- PostgreSQL ??????? ---

# URL ????, 5432 ? PostgreSQL ?????
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/java_course_db2}

# ?? PostgreSQL ??? (????? 'postgres')
spring.datasource.username=${DB_USERNAME:postgres}

# !!! ????????? PostgreSQL ?? !!!
spring.datasource.password=${DB_PASSWORD:change-me}

# PostgreSQL ?????
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA (Hibernate) ?? ---

# ??????? PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ???????, ??? Hibernate ?????
spring.jpa.hibernate.ddl-auto=validate

# ????????, ????
spring.jpa.show-sql=true
# application.properties
spring.flyway.placeholder-replacement=false
# --- AI ???? (V2.0) ---

# (??) ?????????
# 'mock' = ?? MockAiChatService (????)
# 'real' = ?? RealAiChatService (?? AI)
# (??????? mock)
ai.service.mode=${AI_SERVICE_MODE:mock}

# (??????? API Key ???)
ai.llm.api-key=${AI_LLM_API_KEY:}

jwt.secret=${JWT_SECRET:change-this-jwt-secret-before-running-in-production}


# --- ?? (Cache) & Redis ?? ---
# (?? Spring Cache ???????? redis)
spring.cache.type=redis

# (Redis ??????)
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}

# (??????????? TTL, e.g., 1 ??)
spring.cache.redis.time-to-live=3600000

# ? Spring Boot ? 500 ??? JSON ??? 'message' ??
server.error.include-message=always
